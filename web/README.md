# Bookstr web

Bookstr's web app is a single plain HTML, CSS, and JavaScript file: `index.html`.
It has no React build, npm dependencies, or backend requirement. Run `../webdev.sh`
from this directory's parent to serve it locally on port `11111`.
